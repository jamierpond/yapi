yapi: v1
url: http://localhost:3000/manifest.webmanifest
method: GET
expect:
  status: 200
  assert:
    - '.headers["content-type"] | test("application/manifest")'
    - '.body | fromjson | .name == "yapi"'
